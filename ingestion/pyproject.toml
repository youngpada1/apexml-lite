[project]
name = "ingestion"
version = "0.1.0"
description = "OpenF1 API ingestion into Snowflake RAW schema"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27.0",
    "snowflake-connector-python[pandas]>=3.12.0",
    "pandas>=2.0.0",
]

[tool.uv]
package = false
