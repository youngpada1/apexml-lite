[project]
name = "dbt"
version = "0.1.0"
description = "dbt transformations for ApexML-Lite"
requires-python = ">=3.11,<3.14"
dependencies = [
    "dbt-snowflake>=1.8.0,<1.10.0",
]

[tool.uv]
package = false
